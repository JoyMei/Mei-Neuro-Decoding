The project uses EEG signals from the DEAP Dataset to classify emotions into 4 classes 
using Ensembled 1-D CNNs, LSTMs and 2D , 3D CNNs and Cascaded CNNs with LSTMs.
该项目使用来自DEAP数据集的脑电信号，
使用一维 CNN，LSTM和二维和三维 CNN和带有LSTM的级联CNN将情绪分为4类